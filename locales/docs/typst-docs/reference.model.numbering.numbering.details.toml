
[[main]]
en = """Applies a numbering to a sequence of numbers."""

[[main]]
en = """A numbering defines how a sequence of numbers should be displayed as
content. It is defined either through a pattern string or an arbitrary
function."""

[[main]]
en = """A numbering pattern consists of counting symbols, for which the actual
number is substituted, their prefixes, and one suffix. The prefixes and the
suffix are repeated as-is."""

[[main]]
en = """# Example
```example
#numbering(\"1.1)\", 1, 2, 3) \\
#numbering(\"1.a.i\", 1, 2) \\
#numbering(\"I – 1\", 12, 2) \\
#numbering(
  (..nums) => nums
    .pos()
    .map(str)
    .join(\".\") + \")\",
  1, 2, 3,
)
```"""

[[main]]
en = """# Numbering patterns and numbering functions
There are multiple instances where you can provide a numbering pattern or
function in Typst. For example, when defining how to number
[headings]($heading) or [figures]($figure). Every time, the expected format
is the same as the one described below for the
[`numbering`]($numbering.numbering) parameter."""

[[main]]
en = """The following example illustrates that a numbering function is just a
regular [function] that accepts numbers and returns [`content`].
```example
#let unary(.., last) = \"|\" * last
#set heading(numbering: unary)
= First heading
= Second heading
= Third heading
```"""
